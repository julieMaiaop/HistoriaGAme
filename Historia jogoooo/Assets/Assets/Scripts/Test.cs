using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour
{


    private void Start()
    {
        /*
        int random = 0;
        //Enquanto essa condição for verdadeira, repita
        while (random != 2)
        {
            random = Random.Range(0, 5);
            print(random);
        }
        */
        /*
        int num = 5;
        while (num > 0)
        {
            num--;
            print(num);
        }
        */
        /*
        for (int i = 5; i >= 0; i--)
        {
            print("O índice é "+i);
        }
        */
    }
}
